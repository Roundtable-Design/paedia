# Copy to .env and fill in locally. Never commit .env.

# FlutterFlow CLI (rotate token at https://app.flutterflow.io/account)
FLUTTERFLOW_API_TOKEN=
FLUTTERFLOW_PROJECT=paedia-fqv6h9

# Sentry error monitoring — pass at run/build time, never commit the DSN
# flutter run --dart-define=SENTRY_DSN=https://YOUR_KEY@oXXXX.ingest.sentry.io/XXXX
# See docs/MONITORING.md

# Firebase project (read-only audit / future deploys — see docs/PRODUCTION_SAFETY.md)
FIREBASE_PROJECT=paedia-fqv6h9

